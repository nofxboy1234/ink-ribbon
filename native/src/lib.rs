//! Shared map code for the Sokol map example: the editable [`scene::Scene`]
//! model, the [`bake`] pipeline that turns it into runtime assets, and small
//! [`raster`] helpers.

pub mod bake;
pub mod raster;
pub mod scene;
